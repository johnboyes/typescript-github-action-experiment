name: End-to-end test

on:
  push:
  pull_request:

jobs:
  end_to_end:
    name: Run local action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke action
        uses: ./
